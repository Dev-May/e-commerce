import { Helmet } from "react-helmet";
import styles from "./Cart.module.css";

export default function Cart(props) {
  return (
    <div>
      <Helmet>
        <title>Cart</title>
      </Helmet>
      cart
    </div>
  );
}
